<h1><%= @post.title %></h1>

<div class="row">
	<div class="span8">
		<p><%= @post.body %></p>
	</div>
	<div class="span4">
		<% if can? :edit @post %>
			<%= link_to "Edit", edit_post_path(@post), class: 'btn btn-mini' %>
		<% end %>
	</div>
</div>